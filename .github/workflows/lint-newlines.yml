name: Final new line
on:
  pull_request:
    types: [synchronize, opened]
jobs:
  add-newlines:
    name: Add Newlines
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: newline-action
      uses: Labanoras/newline-action@master
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }} # The `GITHUB_TOKEN` secret.